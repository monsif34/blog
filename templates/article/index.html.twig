{% extends 'base.html.twig' %}

{% block title %}Hello ArticleController!{% endblock %}

{% block body %}

    {% if articles is defined %}
        {% for article in articles %}
            Hey voici l'article {{ article.nom  }}<br>
        {% endfor %}
    {% endif %}

    {% if articlesNumber is defined  %}

        {{ dump(articlesNumber) }}
    {% endif %}

    {% if form is defined %}
        {{ form(form) }}
    {% endif %}

{% endblock %}
